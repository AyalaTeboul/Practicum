export default class Person {
    constructor(public personId: number, public firstName: string, public lastName: string, public hmoid: number, public idNumber: string,
        public dateOfBirth: Date, public maleOrFemale: Boolean) { }
}
